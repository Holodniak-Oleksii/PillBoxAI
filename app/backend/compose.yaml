name: pillbox

services:
  postgres:
    image: postgres:16-alpine
    container_name: pillbox-postgres
    environment:
      POSTGRES_DB: pillbox
      POSTGRES_USER: pillbox_user
      POSTGRES_PASSWORD: pillbox_password
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: