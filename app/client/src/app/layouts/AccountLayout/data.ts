import { PATHS } from "@/app/router/paths";

export const matchPaths = {
  [PATHS.NOTIFICATIONS]: "titles.notifications",
  [PATHS.SETTINGS]: "titles.settings",
  [PATHS.ANALYTICS]: "titles.analytics",
};
